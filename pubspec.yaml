name: onboarding
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0
  pin_code_fields: ^7.0.0
  http_requests: ^3.2.0
  image_picker: ^1.1.2
  fluttertoast: ^8.2.8
  connectivity_plus: ^6.0.5


flutter:
  assets:
    - assets/assets/img1.png
    - assets/assets/img2.png
    - assets/assets/img3.png
    - assets/assets/google.png

  uses-material-design: true

